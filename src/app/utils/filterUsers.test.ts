// import { User } from '../types/user';

// const mockUsers: User[] = [
//   { id: 1, name: 'Nguyễn Văn A', email: 'a@example.com' },
//   { id: 2, name: 'Trần Thị B', email: 'b@example.com' },
// ];

// const filterByName = (users: User[], searchTerm: string) =>
//   users.filter((u) => u.name.toLowerCase().includes(searchTerm.toLowerCase()));

// test('lọc người dùng theo tên', () => {
//   const result = filterByName(mockUsers, 'nguyễn');
//   expect(result).toHaveLength(1);
//   expect(result[0].name).toBe('Nguyễn Văn A');
// });
